{
  "name": "platform-hashi-at-home",
  "type": "module",
  "scripts": {
    "build:css": "sass --load-path=node_modules public/styles.scss public/styles.css",
    "dev": "npm run build:css && wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.13.8",
    "@picocss/pico": "^2.1.1"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "sass": "^1.104.1",
    "wrangler": "^4.135.0"
  }
}
